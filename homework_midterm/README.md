# This homework is a WIP

Todo:
- Make containers and docker-compose files for the flask app and redis
- comment code and write readme

docker-compose -p bradenp up -d

docker images | grep braden
docker rmi